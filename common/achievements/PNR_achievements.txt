unique_id = penglai_achievements

QIE_therefore_the_Kalki_descends_achievement = {
	possible = {
		tag = QIE
	}

	happened = {
		if = {
			limit = {
				NOT = { has_global_flag = disallow_achievements_flag }
				OR = {
					NOT = { has_global_flag = on_startup_completed }
					is_ai = no
				}
			}
			has_country_flag = qie_wushan_has_control_china
		}
		else_if = {
			limit = { has_global_flag = disallow_achievements_flag }
			custom_override_tooltip = {
				tooltip = achievements_disabled
				always = no
			}
		}
		else = {
			custom_override_tooltip = {
				tooltip = achievements_disabled_observer
				always = no
			}
		}
	}
}

KOR_mugunghwa_kkochi_pieot_seumnida_achievement = {
	possible = {
		tag = QIE
	}

	happened = {
		if = {
			limit = {
				NOT = { has_global_flag = disallow_achievements_flag }
				OR = {
					NOT = { has_global_flag = on_startup_completed }
					is_ai = no
				}
			}
			KOR = {
				OR = {
					has_completed_focus = KOR_npr_all_people_live_in_peace
					has_completed_focus = KOR_pnr_the_cultural_renaissance_of_the_korean_empire
					has_completed_focus = KOR_pnr_the_eastern_celestial_world
					has_completed_focus = KOR_pnr_the_member_of_asia_order
					has_completed_focus = KOR_pnr_rurals_community
					has_completed_focus = KOR_pnr_the_granary_of_east_asia
				}
			}
		}
		else_if = {
			limit = { has_global_flag = disallow_achievements_flag }
			custom_override_tooltip = {
				tooltip = achievements_disabled
				always = no
			}
		}
		else = {
			custom_override_tooltip = {
				tooltip = achievements_disabled_observer
				always = no
			}
		}
	}
}